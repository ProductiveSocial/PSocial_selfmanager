package com.productivesocial.constants

enum class Priority {
    High, Medium, Low, None
}

enum class TaskSelectionTypes {
    Task, Habit, Routine
}

enum class Recurrency {
    None, Daily, Weekly, Monthly, Yearly
}

enum class HabitType {
    Start, Quit
}